# smokescreen theme: default
# Chthonic gradient-cloud (domain-warped fBm, obsidian/oxblood/plum/verdigris
# with molten-ember veins) + CRT scanlines & bloom on top.
#
# Install: copy this theme's shaders into ~/.config/ghostty/shaders/ and add
# these lines to ~/.config/ghostty/config (or include this file).

background-opacity = 0.0
background-blur = 24

custom-shader = shaders/gradient-cloud.glsl
custom-shader = shaders/crt-bloom.glsl
custom-shader-animation = true
